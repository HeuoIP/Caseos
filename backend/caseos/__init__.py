"""CaseOS runtime package (Sprint 19.1 skeleton).

This package is the **runtime** counterpart of the architecture
documents in `docs/architecture/`. The contracts (ADR-013..018) are
frozen; this package only consumes them.

Status: skeleton. Each intelligence module here is a placeholder
that returns a structured object. Real reasoning will plug in via the
module interfaces defined in `caseos.intelligence.*`.
"""
__version__ = "0.19.1"